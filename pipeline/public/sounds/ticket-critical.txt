TODO: replace this placeholder with `ticket-critical.mp3` — a short (~200ms) ping
sound played when a Critical-priority ticket is assigned to the current user.

The Phase 5 CriticalSound component (src/pipeline/components/tickets/CriticalSound.tsx)
loads `/sounds/ticket-critical.mp3` and falls back silently if missing, so the app
won't crash without this file — but the audible alert is part of the Phase 5 spec
and must be in place before shipping to prod.

Suggested royalty-free options:
  - https://freesound.org (search "notification ping" — CC0)
  - https://mixkit.co/free-sound-effects/notification/
Drop a small MP3 (under 30 KB, mono, ≤44.1 kHz) at this path.
