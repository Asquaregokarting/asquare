import { describe, it } from 'vitest'

describe('ShiftsModule', () => {
  it.todo('renders my shifts view — needs shiftsApi subscription mock (monolith)')
  it.todo('renders team view — needs shiftsApi subscription mock (monolith)')
  it.todo('renders reports view — needs shiftsApi subscription mock (monolith)')
  it.todo('renders leave view — needs shiftsApi subscription mock (monolith)')
})
