import { describe, it } from 'vitest'

describe('CouponsModule', () => {
  it.todo('renders list view — needs deep transitive mock (monolith)')
  it.todo('renders game coupons view — needs deep transitive mock (monolith)')
  it.todo('renders user coupons view — needs deep transitive mock (monolith)')
})
